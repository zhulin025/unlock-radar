# 全球股票解禁日历：产品与落地规划

> 版本：v1.0（规划稿）  
> 覆盖市场：A 股、港股、美股  
> 产品定位：面向个人投资者和研究人员的“未来供给冲击事件日历”，不是交易建议，也不把“解禁”直接等同于“减持”。

## 1. 产品目标

建立一个以月历为核心的股票解禁信息网站，让用户在 10 秒内回答三个问题：

1. 某一天有哪些公司存在解禁或锁定期到期事件？
2. 解禁多少股、占总股本和当前流通股本多少？
3. 这条数据来自哪里，是公司公告确认，还是按公开条款推算？

首版优先解决“可查、可信、可解释”，不承诺判断解禁后一定发生卖出，也不直接输出买卖信号。

## 2. 核心口径

### 2.1 统一定义

- **解禁事件**：原先受出售、转让或上市流通限制的股份，在某日期起解除限制或获准上市流通。
- **公告确认日期**：交易所或上市公司公告明确给出的上市流通日、禁售期届满日。
- **推算日期**：根据 IPO 日期、招股书或 lock-up agreement 中的期限和条件计算出来的日期。
- **解禁不等于减持**：股份变为可流通，不代表持有人一定卖出。
- **流通股本**：按各市场可取得的最新公开口径保存，并显示其数据日期；禁止用总股本减本次解禁数简单倒推。

### 2.2 三地市场差异

| 市场 | 首版纳入事件 | 日期性质 | 主要难点 |
|---|---|---|---|
| A 股 | 首发原股东限售股、定增限售股、股权激励限售股等上市流通 | 多数可由公告确认 | 公告修订、分批解禁、送转后数量调整 |
| 港股 | 控股股东及基石投资者禁售期届满、配售或认购的自愿锁定到期 | 公告确认与条款推算并存 | 披露分散在招股书和后续公告，未必统一公布“解禁数量” |
| 美股 | IPO lock-up、增发或私募 lock-up、部分注册权相关可售日期 | 多数需从 SEC 文件解析或推算 | 90/180 天之外还有财报窗口、提前解除、股价条件和承销商豁免 |

### 2.3 数据状态与置信度

每条事件必须包含状态，前台可见：

- `confirmed`：公告明确确认日期与数量。
- `calculated`：依据公开条款计算，日期尚未被后续公告确认。
- `conditional`：日期受价格、业绩、财报发布或其他条件影响。
- `changed`：原计划已被修订或延期，保留变更记录。
- `cancelled`：事件取消，不再计入当天汇总但详情可追溯。

置信度建议显示为“已确认 / 按条款推算 / 条件性事件”，不使用含义模糊的星级。

## 3. 用户与使用场景

### 核心用户

- 关注短期供给变化的个人投资者。
- 做事件驱动研究的分析人员。
- 需要批量查看未来解禁压力的内容创作者或投研团队。

### 核心任务

- 查看本月或下月解禁密集日。
- 按 A 股、港股、美股筛选。
- 查看某公司本次解禁规模和股份类型。
- 从公司详情回看历史解禁、查看未来批次。
- 点击原始公告复核数据。

## 4. 信息架构

### 4.1 月历首页 `/calendar`

页面顶部：

- 产品名称与全站搜索框，支持公司简称、公司全称和股票代码。
- 上月、今天、下月、月份选择器。
- 市场筛选：全部 / A 股 / 港股 / 美股。
- 事件类型、确认状态、解禁规模筛选。
- 月度摘要：事件数、涉及公司数、解禁股数、按最新价估算市值。

首页收藏公司列表：

- 位于搜索区下方、月历上方，标题为“我的收藏”，同时显示已收藏公司数量。
- 收藏单位固定为公司，不收藏某一天或某一条解禁事件；同一公司无论有多少次解禁都只出现一次。
- 每家公司展示：市场、股票代码、公司简称、下一次解禁日期、下一次解禁数量及占总股本比例。
- 公司当前没有已收录的未来解禁时，显示“暂无未来解禁”，但仍保留在列表中。
- 默认按下一次解禁日期由近到远排列；无未来事件的公司排在末尾。
- 点击公司主体进入公司详情页；点击下一次解禁信息打开对应事件详情。
- 每行提供取消收藏按钮；列表顶部提供“一键清空”，执行前需要二次确认。
- 桌面端默认横向卡片或紧凑表格展示，超过 6 家时折叠并显示“查看全部”；移动端使用横向滚动卡片。
- 无收藏时展示轻量空状态，引导用户通过搜索结果、月历公司行或详情页添加公司。

月历单元格：

- 日期和交易日/休市标识。
- 当日公司列表，默认展示 3 条，超出显示“还有 N 家”。
- 每条展示：市场标识、代码、公司简称、解禁股数、占总股本比例。
- 每条公司记录提供收藏按钮，收藏状态在月历、搜索结果和详情页保持同步。
- 高压力事件可用柔和的颜色层级标识，但不能用红绿暗示涨跌预测。
- 点击公司行打开详情抽屉；点击“还有 N 家”打开当日完整列表。

移动端：

- 默认仍保留月历，但单元格只显示事件数量和压力标识。
- 点击日期后在下方展示当天公司列表。
- 提供“列表视图”切换，避免小屏强塞公司名称。

### 4.2 首页搜索

- 搜索框固定在首页顶部，输入公司简称、公司全称或股票代码即可搜索。
- 支持中文、英文、港美股代码以及 A 股纯数字代码；代码精确匹配优先于名称模糊匹配。
- 输入 2 个字符后触发联想，使用 250–300ms 防抖；纯数字股票代码可从第 1 个字符开始联想。
- 搜索结果展示市场、代码、公司名、最近一次和下一次解禁日期，以及收藏按钮。
- 点击结果：有未来事件时进入最近一条未来解禁详情；没有未来事件时进入公司详情页。
- 结果按“代码精确匹配 > 名称前缀匹配 > 名称包含匹配”排序，并允许按市场缩小范围。
- 无结果时明确提示“未找到已收录公司”，并提供清除搜索入口。
- 搜索关键词可写入 URL 查询参数，方便刷新和分享；不在浏览器中永久保存搜索历史。

### 4.3 本地收藏

- 用户可在月历公司行、搜索结果、事件抽屉和公司详情页收藏或取消收藏公司；任何入口操作的对象始终是公司，而不是单次解禁事件。
- 不接入账户系统，收藏保存到浏览器 `localStorage`。
- 收藏对象只保存稳定标识和必要展示快照，例如 `securityId`、市场、代码、公司名、收藏时间；解禁数据始终从服务端读取，避免本地旧数据误导用户。
- 首页通过独立的“我的收藏”列表展示收藏公司，不将收藏状态与当前月历筛选绑定。
- 没有收藏时展示引导态，说明可通过公司行、搜索结果或详情页的星标添加。
- 本地收藏只在当前浏览器和当前设备有效；清除浏览器数据、无痕模式结束或更换设备后不会保留。
- 首版不提供跨设备同步；可以提供“一键清空收藏”，不需要导入导出。
- 页面初次渲染后再读取本地收藏，避免服务端渲染与客户端状态不一致。

建议本地存储键：`unlock-calendar:favorites:v1`。数据结构：

```json
{
  "version": 1,
  "items": [
    {
      "securityId": "cn-sh-600000",
      "market": "CN",
      "symbol": "600000",
      "companyName": "示例公司",
      "createdAt": "2026-07-22T08:00:00.000Z"
    }
  ]
}
```

### 4.4 公司事件详情抽屉

首屏信息：

- 公司简称、全称、股票代码、交易所、行业。
- 事件日期、状态、最后核验时间。
- 本次解禁股数。
- 占总股本比例。
- 占解禁前流通股比例。
- 解禁后预计流通股本及比例。
- 按最新可得收盘价估算的解禁市值，并明确价格日期和币种。

补充信息：

- 股份类型：首发原股东、定增、股权激励、基石投资者、IPO lock-up 等。
- 股东或持有人列表及各自数量（有披露时）。
- 解禁条件、例外条款和风险说明。
- 原始公告/招股书/SEC filing 链接及关键页码或章节。
- 数据修订历史。
- “查看公司完整详情”入口。
- 收藏/取消收藏按钮。

### 4.5 公司详情页 `/stocks/:market/:symbol`

- 公司简介和基础股本信息。
- 未来解禁批次时间轴。
- 历史解禁记录。
- 当前总股本、流通股本及数据日期。
- 原始资料列表。
- 明确免责声明：解禁不代表实际出售。
- 收藏/取消收藏按钮，状态与首页同步。

### 4.6 数据说明页 `/methodology`

公开说明三地市场定义、来源、计算公式、更新时间、异常处理和更正渠道。这是建立可信度的必要页面，不是附属页面。

## 5. 计算公式与展示规则

设：

- `unlock_shares`：本次解禁股数。
- `total_shares_before`：事件前最近可得总股本。
- `float_shares_before`：事件前最近可得流通股本。

计算：

- 占总股本比例 = `unlock_shares / total_shares_before × 100%`
- 占解禁前流通股比例 = `unlock_shares / float_shares_before × 100%`
- 解禁后预计流通股本 = `float_shares_before + unlock_shares`，但仅在股份确实转为该口径下的流通股时计算。
- 解禁后预计流通比例 = `预计流通股本 / total_shares_before × 100%`
- 估算解禁市值 = `unlock_shares × 最近可得收盘价`

规则：

- 分母的基准日期必须展示。
- 存在多股权类别时，保留原始 share class，不强行合并。
- ADR 必须区分 ADS 和底层普通股，并保存换算比例。
- 港币、美元、人民币不在默认总数中直接相加；需要总计时先按标注日期汇率折算。
- 数量未知时显示“未披露”，不能填 0。
- 条件性到期日不进入“已确认解禁规模”汇总。

## 6. 数据来源与采集方案

### 6.1 原则

数据源优先级：交易所/监管机构/公司公告 > 授权数据供应商 > 人工复核录入。任何第三方行情或聚合数据都不能覆盖原始文件事实。

### 6.2 A 股

- 上交所、深交所、北交所上市公司公告。
- 识别“限售股份上市流通”“首次公开发行部分限售股上市流通”等公告。
- 结构化提取：上市流通日期、数量、股份类型、持有人、总股本变化表。
- 对修订公告、延期公告和同一事件重复公告做关联与版本覆盖。

建议：MVP 可先接入有授权的结构化供应商完成全量日历，同时用交易所公告做来源回链与抽样核验；直接长期爬取前必须确认各站点服务条款、频率限制和数据授权。

### 6.3 港股

- HKEXnews 上市文件、招股章程、配售公告、禁售承诺及后续变更公告。
- IPO 时建立锁定批次；后续公告用于修订日期、数量和持有人。
- 区分上市规则要求、基石投资协议、自愿锁定承诺。
- 不能只用“上市日 + 6 个月”生成确定事件，必须保存原文条件和推算依据。

### 6.4 美股

- SEC EDGAR 的 S-1/F-1、424B、8-K、6-K、注册权协议和 lock-up agreement 等文件。
- 保存发行日期、锁定天数、财报窗口规则、提前解除条款、承销商豁免和持有人范围。
- EDGAR 披露通常不会提供一个标准化的“解禁日历”字段，因此需要规则引擎和人工复核。
- MVP 建议只覆盖近 24 个月 IPO 的标准 lock-up 事件，并把复杂条件事件标为 `conditional`；后续再扩展 PIPE、SPAC 和二次发行。

### 6.5 行情与股本快照

- 行情：日线收盘价、币种、复权状态。
- 股本：总股本、流通股本、自由流通股本需分字段保存，不混用。
- 每个快照包含 `as_of_date`、来源和采集时间。
- 正式商业化前应选择有明确展示与再分发授权的数据供应商。

### 6.6 数据流水线

1. 定时抓取公告索引和新文件元数据。
2. 文档下载、哈希去重、文本解析/OCR。
3. 规则抽取候选事件。
4. 标准化证券、股份类别、数量、日期和来源。
5. 计算衍生指标并执行数据校验。
6. 低置信度或冲突事件进入人工复核队列。
7. 发布新版本，保留审计记录。
8. 前台 API 缓存失效并更新日历。

LLM 可以用于候选字段抽取和条款摘要，但不能直接作为最终事实来源；所有结果必须受 schema、规则校验和来源定位约束。

## 7. 数据模型

### `securities`

- `id`, `market`, `exchange`, `symbol`, `isin`
- `company_name`, `company_name_en`, `industry`
- `security_type`, `share_class`, `currency`
- `ads_ratio`, `status`

### `unlock_events`

- `id`, `security_id`, `event_date`
- `event_type`, `status`, `confidence`
- `unlock_shares`, `share_unit`
- `total_shares_snapshot_id`, `float_shares_snapshot_id`
- `percent_total`, `percent_float_before`
- `estimated_value`, `price_snapshot_id`
- `date_rule_text`, `conditions_text`
- `first_published_at`, `last_verified_at`, `version`

### `unlock_holders`

- `event_id`, `holder_name`, `holder_type`, `unlock_shares`

### `capital_snapshots`

- `security_id`, `as_of_date`
- `total_shares`, `float_shares`, `free_float_shares`
- `source_document_id`

### `source_documents`

- `id`, `authority`, `document_type`, `title`
- `published_at`, `source_url`, `local_hash`
- `filing_id`, `language`, `extracted_text_path`

### `event_revisions`

- `event_id`, `changed_fields`, `old_value`, `new_value`
- `reason`, `source_document_id`, `created_at`

## 8. 技术架构建议

### 前端

- Next.js + TypeScript。
- Tailwind CSS 或 CSS Modules；月历建议自研网格，不依赖偏会议日程导向的重型组件。
- TanStack Query 管理筛选、月份切换和缓存。
- 使用轻量客户端 store 封装收藏状态，并通过 `storage` 事件同步同一浏览器的多个标签页。
- 对 `localStorage` 读写做 schema 校验和异常降级；存储不可用时页面仍可正常浏览。
- 桌面端使用右侧抽屉，移动端使用底部面板。
- 首屏服务端渲染或预生成月度数据，利于 SEO 和分享。

### 后端

- FastAPI（Python）负责 API、解析任务和规则引擎；Python 更适合文档、表格和金融数据处理。
- PostgreSQL 存储事件、快照、来源和版本。
- Redis 用于月历接口缓存和任务去重。
- 后台任务可从轻量 cron + worker 起步，规模增长后迁移到队列系统。
- 原始公告和解析结果存对象存储，数据库只保存元数据和定位信息。

### API 草案

- `GET /api/calendar?month=2026-08&markets=CN,HK,US`
- `GET /api/events/:id`
- `GET /api/dates/:date/events`
- `GET /api/securities/:id`
- `GET /api/securities/:id/unlock-events`
- `GET /api/securities/summaries?ids=`：批量返回收藏公司的最新信息和下一次解禁，避免逐家公司请求。
- `GET /api/search?q=`
- `GET /api/methodology`

月历接口只返回轻量摘要；详情点击后再加载完整来源、持有人和修订记录。
搜索接口默认最多返回 10 条结果，并要求服务端按市场权限和证券状态过滤。收藏关系完全由前端管理，不新增收藏写入 API；首页只用本地保存的公司 ID 调用批量摘要接口取得最新解禁信息。

### 管理后台

- 待复核事件队列。
- 原文与抽取字段对照。
- 合并重复事件、修订日期/数量、标记取消。
- 发布前校验和双人复核字段。
- 数据缺失、解析失败和即将到期但未确认事件告警。

## 9. 视觉与交互方向

- 视觉基调：金融信息工具，清晰、中性、可信，避免“荐股软件”风格。
- 市场用固定标签区分：A / HK / US；颜色只辅助识别，文字必须同时存在。
- 单元格优先显示公司与规模，不堆叠行业、市值等次要信息。
- 比例统一保留两位小数；数量使用万/百万/亿并支持悬停查看精确值。
- 所有估算值带 `≈`，所有推算日期带“推算”标签。
- 空白日期明确显示“暂无已收录事件”，避免用户误解为采集故障。

## 10. 非功能要求

- 可用性：桌面端和移动端均可完成月份浏览、筛选和详情查看。
- 性能：月历首屏 P75 LCP < 2.5 秒，月份切换缓存命中时 < 500ms。
- 可访问性：键盘可操作、焦点可见、颜色对比达 WCAG AA、抽屉具备正确语义。
- SEO：每月日历和公司事件可索引，结构化标题与描述。
- 安全：后台独立鉴权；对外 API 限流；源文件解析在隔离任务中执行。
- 审计：任何前台数据都能追溯到来源文档和事件版本。
- 合规：展示数据来源、更新时间、授权范围与非投资建议声明。

## 11. 分期交付

### Phase 0：口径与数据验证（1–2 周）

- 确认商业用途、目标覆盖范围和供应商授权。
- 各市场抽取 30–50 个真实事件建立黄金样本。
- 验证股本、日期、股份类型和来源链接的完整率。
- 确定港美股的推算规则边界。

验收：黄金样本关键字段准确率 ≥ 98%，每条可追溯到原文。

### Phase 1：A 股 MVP（3–4 周）

- 完成月历、筛选、日期列表、事件抽屉、公司页。
- 完成公司搜索、本地公司收藏和首页“我的收藏”列表。
- 接入 A 股未来 12 个月与历史 12 个月数据。
- 上线来源回链、更新时间、修订历史和管理后台。
- 建立每日增量更新和异常告警。

验收：公告确认事件覆盖率 ≥ 95%；日期和数量准确率 ≥ 99%；月历核心流程移动端可用。

### Phase 2：港股（2–3 周）

- 接入招股书及锁定条款解析。
- 增加“确认/推算/条件性”展示。
- 支持基石投资者、控股股东和自愿锁定分类。

### Phase 3：美股标准 IPO lock-up（3–4 周）

- 接入 SEC EDGAR 文件与发行事件。
- 先覆盖标准期限，复杂条件进入人工复核。
- 支持 ADS 换算、多股权类别和美股财报窗口规则。

### Phase 4：增强功能（按优先级）

- 账户系统、收藏跨设备同步与事件提醒（邮件、日历订阅、站内提醒）。
- 列表视图、周视图和规模排行。
- CSV 导出及只读 API。
- 事件前后价格表现研究，但与事实数据明确分层。
- 港美股 PIPE、SPAC、二次发行等复杂事件。

## 12. MVP 明确范围

首个可上线版本建议只做：

- A 股完整月历。
- 港股和美股可先保留市场入口并标注“建设中”，或仅展示已人工核验的少量事件。
- 月份切换、市场/类型/状态筛选。
- 首页公司搜索，支持名称和股票代码。
- 无账户的公司收藏、首页“我的收藏”列表、取消收藏和一键清空。
- 当日公司列表和公司事件抽屉。
- 事件数量、占总股本、占流通股本、预计解禁后流通股本。
- 公司简介、来源文档、更新时间、修订记录。
- 数据管理后台和每日更新任务。

首版不做：

- 买卖建议、涨跌预测和所谓“解禁风险评分”。
- 未经授权的大规模行情再分发。
- 把港美股所有员工 RSU vesting 都当作公开可预测解禁。
- 社区、模拟交易、复杂组合分析。
- 登录注册、收藏跨设备同步和消息提醒。

## 13. 验收测试清单

### 数据

- 同一公司同日多批次不会误去重。
- 修订公告能够覆盖旧日期且保留历史。
- 未披露数量显示未知而不是 0。
- 总股本、流通股本、价格均显示数据日期。
- ADS、多股权类别、拆股、送转等换算正确。
- 时区统一：事件按所属交易所本地日期展示。

### 产品

- 月份跨年切换正确。
- 筛选条件同步到 URL，可刷新和分享。
- 搜索支持名称与代码，精确代码结果优先，空结果状态正确。
- 收藏在刷新页面后保留，月历、搜索和详情中的状态一致。
- 首页收藏列表按公司去重；收藏同一公司的不同事件不会产生重复公司。
- 收藏列表正确显示每家公司的下一次解禁；没有未来事件时仍保留公司并显示空状态。
- 收藏列表默认按下一次解禁日期排序，超过展示上限后能够展开全部。
- 取消收藏和清空收藏不会影响其他站点数据；禁用本地存储时页面可降级使用。
- 多标签页增删收藏后状态能够同步。
- 点击公司打开正确事件，不因同名公司串数据。
- 移动端可完成全部核心动作。
- 推算和已确认事件在视觉与文案上明确区分。
- 原始来源链接可访问，失效时有备份定位信息。

### 性能与稳定性

- 一个月 1,000 条事件仍可流畅展示。
- 抓取失败不删除已发布数据。
- 数据任务可重跑且不会重复插入。
- API 和后台均有监控、错误日志与告警。

## 14. 主要风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| 把解禁误解为实际减持 | 误导用户 | 全站固定说明；不输出确定性卖出判断 |
| 港美股日期由复杂条款决定 | 日期可能变化 | 状态分级、保存公式、人工复核、持续追踪后续文件 |
| 股本口径不统一 | 比例失真 | 总股本/流通股/自由流通股分开存储，显示基准日 |
| 公告修订或延期 | 旧日历过时 | 增量抓取、事件版本、变更告警 |
| 数据授权与再分发限制 | 无法商业上线 | Phase 0 完成授权评估，优先官方来源与持牌供应商 |
| 文档解析错误 | 数量或日期错误 | 黄金样本、规则校验、低置信度人工复核 |

## 15. 需要在开工前锁定的产品决策

1. 网站是公开免费工具、付费订阅产品，还是内部投研工具。
2. 首次发布是否接受“A 股全量 + 港美股人工核验小范围”的渐进方案。
3. 是否已有可合法使用的行情、股本和公司简介数据源预算。
4. 默认展示“占总股本”还是更强调“占解禁前流通股本”。建议两者都展示，排序以占流通股本为主。
5. 首版不做登录和提醒；收藏使用浏览器本地存储，后续有明确需求再升级为账户同步。

## 16. 推荐的开工顺序

1. 用真实公告做 100 条跨市场样本库，而不是先画完整 UI。
2. 定稿统一事件 schema 和口径说明。
3. 做可点击的月历前端原型，以样本数据验证信息密度。
4. 建 A 股采集、复核、发布闭环。
5. 再逐步接入港股和美股，保持状态分级与来源透明。

这套顺序可以尽早得到可用产品，同时避免三地数据差异拖垮首版。
