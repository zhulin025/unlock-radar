# 解禁数据自动化

## 当前覆盖

- A 股：页面 API 按月份实时读取全市场限售股解禁记录，并自动处理分页。
- 港股：每日扫描 HKEXnews 全市场公告，筛选配发结果和 lock-up 公告，解析 PDF 中的锁定数量、总股本比例和届满日期。
- 美股：每日扫描 SEC EDGAR 日索引中的 424B4，定位主文档并解析 `Shares Eligible for Future Sale` 和 lock-up 条款。
- 历史记录：点击 A 股公司时按代码拉取该公司全部历史解禁；港美股从已解析的官方事件库返回历史记录。

## 运行方式

```bash
# 最近 7 天增量同步
npm run data:sync

# 指定时间段回填
npm run data:sync -- --from=2026-01-01 --to=2026-07-22
```

GitHub Actions 工作流 `.github/workflows/sync-unlock-data.yml` 在工作日每日运行，也支持手动指定日期区间回填。

## SEC 配置

自动运行前应在仓库 Secrets 中配置 `SEC_USER_AGENT`，值使用 SEC 要求的可识别格式：

```text
产品或机构名称 contact@example.com
```

## 发布校验

自动解析采用严格的“解析成功才发布”策略：

- 必须识别公司代码和公司名称。
- 必须识别锁定期或明确届满日期。
- 必须识别解禁股份数量。
- SEC 文件存在提前解除条件时标记为“条件性事件”。
- HKEX 的锁定期最后一日会换算为下一日可转让日期。
- 无法稳定解析的文件只输出同步日志，不会写入前台数据。

因此“全市场自动化覆盖”指全市场公告发现和自动解析流程已经覆盖，并不表示每份复杂文件都能无人工复核地形成事件。解析失败或条件不明确的文件应进入后续人工复核队列。
